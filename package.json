{
  "name": "jukebox",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "jukebox": "node --experimental-strip-types src/cli.ts",
    "build:primitives": "node --experimental-strip-types scratch/build-primitives.ts",
    "lab": "./lab/run",
    "test:lab": "python3 -m unittest discover -s tests -v && python3 -m unittest discover -s lab/methods -p 'test_*.py' -v"
  }
}
